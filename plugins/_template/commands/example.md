---
description: What this command does (shown in the slash-command menu)
argument-hint: <what the user types after the command>
---

This is a slash command. Everything below runs as a prompt when someone types
`/example ...`. The user's text is substituted in for `$ARGUMENTS`.

Delete this file if your plugin doesn't need a command.

Task: $ARGUMENTS
